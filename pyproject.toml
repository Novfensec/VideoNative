[build-system]
requires = ["scikit-build-core", "pybind11"]
build-backend = "scikit_build_core.build"

[project]
name = "videonative"
version = "1.0.0"
description = "Native Cross-Platform C++ FFmpeg Video Decoder"